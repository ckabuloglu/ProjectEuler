function [ list ] = prob24( arr )

len = length(arr);
arr = sort(arr);

list = [];

if len == 1
    list = arr(1);
else
    len = length(arr);
    for n = [1:len]
        rep = arr;
        now = rep(n);
        rep(rep==now) = [];
        
    end 
end

end



